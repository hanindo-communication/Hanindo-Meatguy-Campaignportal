# Required in production (min 16 characters). In development a built-in fallback is used.
SESSION_SECRET=replace-with-long-random-string

# Portal password (default in dev: admin123)
PORTAL_PASSWORD=admin123

# Optional: server port (default 3788)
# PORT=3788
